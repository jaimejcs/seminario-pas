# MVC

Execute `python3 run.py` na raiz e selecione MVC. O Controller em `controllers/` instancia o Model em `models/`, que executa as regras. A View em `views/` serializa os resultados para a interface web compartilhada em `multitiers/frontend/`. Trocar cards por tabela demonstra uma alteração de apresentação independente do Model.
